apiVersion: v1
kind: Service
metadata:
  name: corebridge-backend
  namespace: corebridge
spec:
  selector:
    app: corebridge-backend
    version: green   # 트래픽 대상 (blue로 전환 시 변경)
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
